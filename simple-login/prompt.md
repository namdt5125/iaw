prompt 1
```
tạo câu truy vấn khởi tạo data trong sqlite3 tạo users gồm id, username, password
```
prompt 2
```
sử dụng fastapi, uvicorn, sqlite3, hãy tạo 1 website cơ bản (html cơ bản) với các endpoint /ping return pong, /login-form dùng để đăng nhập bằng username và password, endpoint /login sẽ kiểm tra login thành công hoặc thất bại, /logout để đăng xuất
```
prompt 3
```
thêm endpoint /index, sau khi login thì trả về login success
```


